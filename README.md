# dg3-notes
Lecture notes for EPFL MATH344 Differential Geometry 3

Fast compile with
```
pdflatex main
```

or do a full compilation, in case the bibliography has to be built:
```
pdflatex main && biber main && pdflatex main && pdflatex main
```

